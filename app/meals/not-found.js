export default function NotFound(){
    return(
        <main className="not-found">
            <h1>Meal not Found</h1>
            <p>Unfortunately, meal you are looking for is not found.</p>
        </main>
    );
}